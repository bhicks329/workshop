baseregistry-username: ${baseregistry-username}
baseregistry-passwd: ${baseregistry-password}
baseregistry-url: ${baseregistry-url}:443/${baseregistry-name}
aquasec-username: ${aquasec-username}
aquasec-passwd: ${aquasec-passwd}
wrregistry_username: ${wrregistry_username}
wrregistry_passwd: ${wrregistry_passwd}
wrregistry_url: ${wrregistry_url}
k8s-ca: ${k8s-ca}
k8s-server: ${k8s-server}
k8s-token: ${k8s-token}
